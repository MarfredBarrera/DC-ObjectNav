# Field-verified LLMDet — sweep point: threshold 0.10 (prunes only
# cold/uncorroborated detections). τ=0.47 sink-gated LLMDet, no cascade.
detection:
  llmdet_threshold: 0.47
  detector_cascade: false
  field_verify: true
  field_verify_threshold: 0.10
  field_verify_top_frac: 0.10
