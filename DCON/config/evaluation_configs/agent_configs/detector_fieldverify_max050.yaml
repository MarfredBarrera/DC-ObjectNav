# Field-verified LLMDet — sweep point: MAX pooling, threshold 0.50 (loose:
# one well-corroborated in-box point passes). τ=0.47 sink-gated LLMDet,
# no cascade.
detection:
  llmdet_threshold: 0.47
  detector_cascade: false
  field_verify: true
  field_verify_threshold: 0.50
  field_verify_pool: "max"
